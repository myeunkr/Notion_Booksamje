# SPEC.md — 노션 약방: 학교생활 고민 유형검사

> 문서 상태: **확정 (구현 진행 중)**
> 이 문서는 사용자가 전달한 전체 요구사항을 분석·검증하고, 구현 착수 전 확인이 필요한 항목을 정리한 것입니다.
> 문항 문구 / 유형명 / 결과 문구는 원문 그대로 유지했습니다. 임의로 요약하거나 표현을 바꾸지 않았습니다.
> §2의 4개 결정 사항은 사용자 확인을 거쳐 아래와 같이 확정되었습니다.
>
> **변경 이력**: 배포 후 "문항이 길다/많다"는 피드백에 따라 ① 15개 문항 문장을 더 짧고
> 직관적인 구어체로 다듬고, ② 문항 수를 15개(모든 조합)에서 9개(균형 잡힌 부분집합)로
> 줄였습니다. 아래 §1은 이 최종(9문항) 설계 기준으로 갱신되어 있습니다.

---

## 0. 프로젝트 개요

- **프로젝트명**: 노션 약방 – 학교생활 고민 유형검사
- **성격**: 부산대학교 축제 부스용 모바일 웹사이트. 전문 심리검사가 아닌, 학교생활 고민 우선순위를 확인하고 Notion 템플릿을 추천하는 콘텐츠.
- **배포 형태**: 백엔드/DB/회원가입 없는 완전한 정적 웹사이트, GitHub Pages 배포 예정(이번 단계에서는 진행하지 않음).
- **기술 조건**: Vite + React + TypeScript(strict), 불필요한 외부 패키지 금지, 별도 라우팅 라이브러리 지양, 데이터/로직/UI 분리, localStorage만 사용(외부 전송 없음), 추적 도구 없음.

---

## 1. 요구사항 검증

### 1-0. 문항 수 축소 (15 → 9)

원래는 6개 유형의 모든 조합(C(6,2)=15개)을 빠짐없이 비교했으나, "문항이 너무 길다"는
피드백에 따라 9개로 줄였습니다. 임의로 6개를 뺀 것이 아니라, 아래 구조로 균형을 유지한
채 축소했습니다.

- 그룹1(자기관리): schedule · study · habit
- 그룹2(정보·관계관리): collaboration · organization · archive
- ① 그룹 내부 조합은 모두 비교 (그룹당 3쌍 × 2그룹 = 6쌍)
- ② 그룹 간에는 schedule↔collaboration, study↔organization, habit↔archive로 짝지어진 1쌍씩만 비교 (3쌍)
- 총 6 + 3 = **9쌍**

이 방식의 핵심은 "그룹 내부는 완전 비교 + 그룹 간은 정확히 1:1 매칭"이라 **6개 유형이
여전히 정확히 같은 횟수(3회)씩 등장**한다는 점입니다. 즉 문항 수는 줄었지만 특정 유형이
더 자주/덜 등장해서 유불리가 생기는 일은 없습니다.

### 1-1. 9개 문항의 조합 검증

| # | 유형 A | 유형 B | 비고 |
|---|---|---|---|
| 1 | schedule | study | 그룹1 내부 |
| 2 | collaboration | schedule | 그룹 간 짝 |
| 3 | habit | schedule | 그룹1 내부 |
| 4 | organization | study | 그룹 간 짝 |
| 5 | study | habit | 그룹1 내부 |
| 6 | collaboration | organization | 그룹2 내부 |
| 7 | collaboration | archive | 그룹2 내부 |
| 8 | archive | organization | 그룹2 내부 |
| 9 | habit | archive | 그룹 간 짝 |

→ **의도한 9개 조합이 중복 없이 정확히 1회씩 존재 (PASS, 검증: tests/data-integrity.test.ts)**

제외된 6개 조합(그룹이 다르면서 짝이 아닌 조합): schedule↔organization, schedule↔archive,
study↔collaboration, study↔archive, habit↔collaboration, habit↔organization.

### 1-2. 유형별 등장 횟수 검증

| 유형 | 등장 문항 | 횟수 |
|---|---|---|
| schedule | 1, 2, 3 | 3 |
| study | 1, 4, 5 | 3 |
| collaboration | 2, 6, 7 | 3 |
| organization | 4, 6, 8 | 3 |
| habit | 3, 5, 9 | 3 |
| archive | 7, 8, 9 | 3 |

→ **6개 유형 모두 정확히 3회 등장 (PASS)**. 합계 18 = 9문항×2선택지 = 6유형×3회로 정합성도 확인.

### 1-3. 위/아래 위치 편향 관련 확인

"각 유형의 위·아래 등장 횟수가 지나치게 치우치지 않게"라는 조건은, 매 세션 시작 시 문항별로 위/아래를 **독립적으로 50%씩 무작위 결정**하는 구조에서는 설계상 자연스럽게 충족됩니다(개별 세션 단위 편차는 통계적으로 불가피하며, 이는 진짜 무작위성의 정상적인 결과입니다). 데이터 파일 자체의 A/B 나열 순서가 어느 한쪽으로 쏠려 있어도 실제 노출 순서는 매번 다시 섞이므로 별도의 데이터 재배치는 필요하지 않다고 판단했습니다. **(확인 필요 항목 아님, 설계 근거로 기록)**

---

## 2. 누락·충돌 가능성이 있던 항목 (확정됨)

아래 항목은 요구사항 자체를 바꾸는 것이 아니라, 명시되지 않았거나 문면상 모호했던 부분에 대한 제안이며, 사용자 확인을 거쳐 **모두 추천안대로 확정**되었습니다. 문항/유형명/결과 문구는 전혀 건드리지 않습니다.

### ✅ 확정된 결정 사항

**(A) 3개 이상 공동 1위 시, "나머지 공동 1위" 중 보조 처방 선택 기준 → 고정 우선순위 방식 확정**
요구사항 문서의 "검사 유형" 섹션에 나열된 순서(schedule → study → collaboration → organization → habit → archive)를 고정 우선순위로 사용해, 남은 공동 1위 유형 중 이 순서상 가장 앞선 유형 하나를 보조 처방으로 자동 선택한다. "2위 유형이 동점인 경우"에도 동일한 고정 순서를 적용한다.

**(B) 동점 결정 화면 제목 문구 → 동점 개수에 따라 동적으로 조정 확정**
동점 유형이 정확히 2개면 "두 가지 고민의 우선순위가 비슷해요.", 3개 이상이면 "고민들의 우선순위가 비슷해요."로 표시한다.

**(C) 최소 테스트 케이스 2번("6개 유형 모두 동점") → 순수 함수에 인위적 점수 배열 직접 주입 방식 확정**
실제 9문항 응답으로는 도달 불가능한 상태이므로(9는 6으로 나누어떨어지지 않음), 동점 처리 로직(순수 함수)에 `{ schedule:2, study:2, ... }`처럼 모든 유형이 2점인 인위적 점수 배열을 직접 주입하여 동점 결정 로직이 올바르게 동작하는지 검증한다.

**(D) 임시 URL 처리 방식 → `{ url: null, ready: false }` 플래그 방식 확정**
`links.ts`에서 각 링크를 `{ url: null, ready: false }` 형태로 관리한다. `ready:false`인 동안은 항상 "링크 준비 중" 안내만 표시하고 실제 이동은 하지 않는다. 이후 실제 URL이 채워지면 `ready:true`로 바꾸고 새 탭(`target="_blank" rel="noopener noreferrer"`)으로 안전하게 열리도록 구현한다.
**→ 이 방식이면 별도로 가짜 URL 문자열을 채울 필요가 없는데, 괜찮을지 확인 부탁드립니다.**

### ℹ️ 가정 후 진행 예정 (이견 없으시면 이대로 구현합니다)

1. **마지막 문항(15번) 선택 시에도 동일하게 약 200ms 지연 후 결과/동점 화면으로 전환**합니다(동작 감소 설정 시 지연 없음, 다른 문항과 동일 규칙 적용).
2. **"처음 화면으로 이동하는 버튼 또는 로고"**를 누르면 진행 중인 답변은 그대로 localStorage에 유지한 채 시작 화면으로 이동합니다(시작 화면에서 "이어서 하기"로 복귀 가능). 초기화는 발생하지 않습니다.
3. **시작 화면의 "새로 시작하기"**도 결과 화면의 "다시 검사하기"와 동일하게 진행 중 데이터를 지우는 동작이므로, 동일한 확인 절차(간단한 확인 다이얼로그)를 적용합니다.
4. **확인 다이얼로그(재검사/새로 시작) 및 공유 문구의 정확한 카피**는 요구사항에 지정되어 있지 않아 아래와 같이 초안을 제시하고, 구현 단계에서 화면에 반영합니다(원하시면 언제든 문구 교체 가능).
   - 초기화 확인: "지금까지의 답변과 진행 상태가 모두 사라져요. 다시 시작할까요?" / 버튼: "취소" · "새로 시작"
   - 공유 문구 템플릿: `"[노션 약방] 나의 처방: {결과명} — {한 줄 처방}"`
5. **브라우저 자체의 뒤로가기 버튼**은 이번 범위에서 별도로 가로채거나 앱 내비게이션과 연동하지 않습니다(라우팅 라이브러리를 쓰지 않는 단일 페이지 상태 머신이므로, 문항 이동은 화면 내 "이전" 버튼으로만 처리).
6. **동점 결정 화면에서 "이전 화면으로 돌아갈 방법"**은 15번 문항의 선택 화면으로 복귀하는 것으로 구현하며, 이 경우 동점 결정 단계에서의 선택(있었다면)은 초기화됩니다.

---

## 3. 점수 계산 & 동점 처리 로직 정리

### 3-1. 기본 원칙
- 점수는 **저장하지 않고**, 저장된 9개 답변 배열로부터 **매번 순수 함수로 재계산**합니다.
- 각 유형 점수 범위: 0~3점(`MAX_SCORE_PER_TYPE`, 문항 수와 유형 수로부터 계산). 전체 합은 항상 9점(모든 문항에 답했을 때).

### 3-2. 처리 흐름

```
1. 모든 문항에 답했는가?
   - 아니오 → 결과 계산하지 않음 (테스트 케이스 6)

2. calculateScores(answers) → { schedule: n, study: n, ... }

3. topTypes = 최고 점수를 가진 유형 목록 (1개 이상)

4. topTypes.length === 1
   → 주 처방 = topTypes[0] (동점 아님)
   → secondaryCandidates = topTypes를 제외한 유형 중 최고점 그룹
      - secondaryCandidates.length === 1 → 보조 처방 = 그 유형
      - secondaryCandidates.length >= 2 → 결정 필요 항목 (A) 적용
            (고정 우선순위 1개 선택 또는 보조 처방 복수 표시)
   → 강도 문구:
        1위-2위 점수차 >= 2 → "현재 가장 우선적인 고민이 비교적 뚜렷하게 나타났어요."
        1위-2위 점수차 == 1 → "두 가지 고민이 함께 나타났으며, 그중 이 처방이 조금 더 우선이에요."

5. topTypes.length >= 2 (공동 1위)
   → 동점 결정 화면 표시
        topTypes.length === 2 → 두 선택지를 비교하는 카드
        topTypes.length >= 3 → "지금 하나만 먼저 해결한다면?" + 해결 목표 카드
   → 사용자가 하나를 선택 → 주 처방 = 사용자가 선택한 유형
   → 나머지 topTypes 중 보조 처방 선택 → 결정 필요 항목 (A) 적용
   → 강도 문구: "두 가지 이상의 고민이 비슷하게 나타나, 지금 먼저 해결하고 싶은 고민을 기준으로 처방했어요."
```

### 3-3. 답변 변경(이전 버튼) 시 정합성
- 점수를 별도 누적하지 않고 매번 answers 배열에서 재계산하므로, 특정 문항의 답변을 바꿔도 이전 점수를 "취소"하는 별도 로직이 필요 없습니다. answers 배열의 해당 인덱스만 교체하면 재계산 시 자동으로 반영됩니다. → **요구사항이 우려한 "취소 오류" 자체가 구조적으로 발생하지 않는 설계**입니다.

### 3-4. localStorage 손상/버전 처리
- 저장 데이터에 `version` 필드 포함. 코드의 기대 버전과 다르면 저장 데이터를 무시하고 초기 상태로 리셋.
- JSON.parse 실패, 스키마 불일치(필드 누락/타입 불일치) 등은 모두 "손상된 데이터"로 간주해 조용히 초기 상태로 폴백(에러를 사용자에게 노출하지 않음).

---

## 4. 모바일 화면 흐름 설계

### 4-1. 화면 목록
1. **시작 화면 (Start)**
2. **검사 화면 (Question)** — 9문항 공용 레이아웃
3. **동점 결정 화면 (TieBreak)** — 공동 1위가 있을 때만 진입
4. **결과 화면 (Result)**

라우팅 라이브러리 없이, 단일 상태(`screen: 'start' | 'question' | 'tie' | 'result'`)로 전환하는 내부 상태 머신으로 구현합니다.

### 4-2. 흐름도

```
[시작 화면]
 ├─ 저장된 미완료 진행 있음
 │    ├─ "이어서 하기" → [검사 화면: 저장된 문항 인덱스]
 │    └─ "새로 시작하기" → (확인 절차) → 초기화 → [검사 화면: 1문항]
 └─ 저장된 진행 없음
      └─ "검사 시작" → [검사 화면: 1문항]

[검사 화면] (1~9문항 공용)
 ├─ 카드 선택 → 선택 상태 표시 → (~200ms, reduced-motion 시 즉시)
 │     ├─ 마지막 문항 아님 → 다음 문항으로 이동
 │     └─ 마지막 문항 → 점수 계산
 │           ├─ 단독 1위 → [결과 화면]
 │           └─ 공동 1위 있음 → [동점 결정 화면]
 ├─ "이전" 버튼 → 이전 문항으로 이동(이미 선택했던 카드 상태 유지, 재선택 가능)
 └─ 로고/처음으로 → [시작 화면] (진행 상태는 유지)

[동점 결정 화면]
 ├─ 해결 목표 카드 선택 → [결과 화면]
 └─ "이전 화면으로" → [검사 화면: 9문항, 재선택 가능]

[결과 화면]
 ├─ "처방 템플릿 열기" → ready 여부에 따라 새 탭 오픈 / "링크 준비 중" 안내
 ├─ "결과 공유하기" → Web Share API 또는 클립보드 복사(결과명 + 한 줄 처방만)
 ├─ "방명록 작성하기" → 동일한 ready 로직
 └─ "다시 검사하기" → (확인 절차) → 전체 초기화 → [시작 화면]
```

### 4-3. 레이아웃 원칙
- **모바일 세로가 기본값**: 콘텐츠는 항상 1열, 좌우 여백(gutter) 최소 20px.
- **태블릿/데스크톱**: 콘텐츠 최대 폭(예: 480~560px)을 지정하고 `margin: 0 auto`로 중앙 정렬, 배경(크림톤)은 화면 전체를 채움.
- **가로 스크롤 금지**: `overflow-x: hidden` + 모든 요소 `max-width: 100%` / `box-sizing: border-box`.
- **높이 기준**: `100dvh`를 우선 사용하고, 미지원 브라우저를 위한 `100vh` 폴백 병기.
- **하단 고정 액션 영역**: 검사 화면의 "이전" 버튼, 시작/결과 화면의 주요 CTA는 화면 하단부(엄지 도달 영역)에 배치하고 `padding-bottom: env(safe-area-inset-bottom)`으로 iOS 하단 제스처 바에 가리지 않게 처리.
- **터치 영역**: 선택 카드 전체(텍스트 여백 포함)가 클릭 가능해야 하며, 버튼/카드 모두 최소 높이 48px 보장.
- **타이포그래피**: 본문 최소 16px, 시스템 폰트 스택 우선 사용(`-apple-system, "Segoe UI", "Noto Sans KR", sans-serif` 등).
- **모션**: 카드 선택 후 다음 문항 전환 정도의 절제된 트랜지션만 사용, `prefers-reduced-motion: reduce`에서는 지연·트랜지션 제거.
- **포커스/키보드**: 모든 인터랙션 요소는 `<button>` 기반, `:focus-visible` 스타일 명시적으로 정의.
- **색상 대비**: 크림/흰/주황/짙은 갈색 팔레트 내에서 텍스트-배경 대비 WCAG AA(4.5:1) 기준 확인 후 확정.

### 4-4. 디자인 콘셉트 반영
- 시작 화면: "처방전 발급 전 접수창구" 느낌의 단순한 카드 레이아웃.
- 검사 화면: 장식 최소화, 문항에 집중되도록 두 개의 큰 선택 카드 중심 구성.
- 결과 화면: 종이 처방전 톤(상단 병원/약국 헤더 형태의 "오늘의 진단", 점선 구분선, 도장 느낌의 포인트 컬러 사용 등, 이미지 없이 CSS만으로 표현).

---

## 5. 기술 스택 제안

| 영역 | 선택 | 이유 |
|---|---|---|
| 빌드 도구 | Vite | 요구사항 명시, 빠른 정적 빌드, GitHub Pages 배포 용이 |
| 프레임워크 | React 18 + TypeScript(strict) | 요구사항 명시 |
| 라우팅 | 없음 (내부 상태 머신) | 화면이 4개뿐이고 전부 하나의 세션 흐름 안에 있어 별도 URL 라우팅이 필요하지 않음. 요구사항의 "꼭 필요하지 않다면 사용하지 마"에 부합 |
| 전역 상태 | React 기본 state/useReducer + 커스텀 훅 | 앱 규모상 별도 상태관리 라이브러리 불필요 |
| 스타일 | 순수 CSS + CSS Modules(Vite 내장 기능) + CSS 변수 | 추가 패키지 없이 컴포넌트 스코프 스타일과 디자인 토큰 관리 가능 |
| 테스트 | Vitest (devDependency만 추가) | Vite와 동일 생태계, 설정 최소. 이번 테스트 대상이 전부 "순수 함수/데이터 무결성"이라 jsdom·Testing Library 등 DOM 테스트 도구는 불필요 → 의존성 최소화 |
| 저장소 | localStorage (버전 필드 포함) | 요구사항 명시 |

→ 추가되는 유일한 신규 의존성은 **Vitest(devDependency)** 이며, 이는 "테스트 도구가 없다면 가벼운 테스트 환경만 추가"라는 요구사항에 따른 최소 추가입니다.

---

## 6. 파일 구조 제안

```
Notion_booksamje/
├── index.html
├── package.json
├── tsconfig.json
├── tsconfig.node.json
├── vite.config.ts
├── SPEC.md
├── public/
│   └── (파비콘 등, 이미지 없이 최소한으로)
├── src/
│   ├── main.tsx
│   ├── App.tsx                     # 화면 상태 머신(start/question/tie/result) 라우팅
│   ├── types.ts                    # TypeId, Question, StoredState 등 공용 타입
│   ├── data/
│   │   ├── questions.ts            # 9문항 데이터(원래 15개 중 균형 잡힌 부분집합)
│   │   ├── resultContent.ts        # 6개 유형의 결과명/한줄처방/설명/템플릿명/동점화면 해결목표
│   │   └── links.ts                # 템플릿 6개 + 방명록 URL 설정 (ready 플래그 포함)
│   ├── logic/
│   │   ├── scoring.ts              # calculateScores / resolveResult 등 순수 함수
│   │   └── storage.ts              # localStorage read/write/버전관리/손상데이터 처리
│   ├── state/
│   │   └── useTestState.ts         # 상태 총괄 훅 (storage 연동, 액션 정의)
│   ├── hooks/
│   │   └── usePrefersReducedMotion.ts
│   ├── screens/
│   │   ├── StartScreen.tsx (+ .module.css)
│   │   ├── QuestionScreen.tsx (+ .module.css)
│   │   ├── TieBreakScreen.tsx (+ .module.css)
│   │   └── ResultScreen.tsx (+ .module.css)
│   ├── components/
│   │   ├── SelectableCard.tsx (+ .module.css)
│   │   ├── PrimaryButton.tsx (+ .module.css)
│   │   ├── ProgressBar.tsx (+ .module.css)
│   │   ├── ScoreBar.tsx (+ .module.css)
│   │   └── ConfirmDialog.tsx (+ .module.css)
│   ├── styles/
│   │   ├── tokens.css              # 색상/타이포/여백 변수
│   │   └── global.css              # 리셋 + 기본 레이아웃(dvh, safe-area 등)
│   └── vite-env.d.ts               # Vite/CSS Modules 타입 참조
└── tests/
    ├── scoring.test.ts             # 점수/동점 로직 테스트
    ├── data-integrity.test.ts      # 9개 조합 완전성 + 유형별 3회 등장 테스트
    └── storage.test.ts             # 손상 데이터 폴백 + 재시작 초기화 테스트
```

---

## 7. 테스트 계획

요구사항의 10개 최소 테스트 사례를 아래와 같이 매핑합니다.

| # | 사례 | 테스트 방식 |
|---|---|---|
| 1 | schedule 단독 1위 | 실제 answers 배열 구성 |
| 2 | 6개 유형 동점 | **실제 답변으로는 불가능(수학적으로 9/6 비정수) → 동점 처리 함수에 인위적 점수 배열 직접 주입** (§2-(C) 확인 필요) |
| 3 | 2개 유형 공동 1위 | 실제 answers 배열 구성 |
| 4 | 3개 이상 유형 공동 1위 | 실제 answers 배열 구성 |
| 5 | 이전 문항 답변 변경 | answers 배열 특정 인덱스 교체 후 재계산 검증 |
| 6 | 미완료 상태에서 결과 미계산 | answers에 null 포함 시 결과 계산 함수가 실행되지 않음을 검증 |
| 7 | 손상된 localStorage | 깨진 JSON 문자열 주입 후 초기 상태로 안전 폴백 검증 |
| 8 | 재시작 시 전체 초기화 | reset 액션 후 저장 데이터가 초기값과 동일한지 검증 |
| 9 | 유형별 정확히 3회 등장 | questions.ts 정적 데이터 검증 (§1-2) |
| 10 | 9개 조합 중복 없이 1회씩 | questions.ts 정적 데이터 검증 (§1-1) |

---

## 8. 구현 순서 체크리스트

- [x] 1. Vite(react-ts) 프로젝트 초기화 및 불필요 보일러플레이트 정리
- [x] 2. tsconfig strict 옵션 확인/보강
- [x] 3. 디자인 토큰 CSS 변수 + 글로벌 스타일(dvh, safe-area, 리셋) 작성
- [x] 4. 공용 타입 정의 (`src/types.ts`)
- [x] 5. 데이터 파일 작성: `questions.ts`, `resultContent.ts`, `links.ts` (원문 그대로)
- [x] 6. 점수 계산/동점 처리 순수 함수 작성 (`logic/scoring.ts`)
- [x] 7. localStorage 유틸 작성 (`logic/storage.ts`) — 버전 관리, 손상 데이터 안전 처리
- [x] 8. Vitest 설정 + `scoring.test.ts` / `data-integrity.test.ts` / `storage.test.ts` 작성 및 통과 확인
- [x] 9. 상태 총괄 훅 작성 (`state/useTestState.ts`)
- [x] 10. 공통 컴포넌트 구현: SelectableCard, PrimaryButton, ProgressBar, ScoreBar, ConfirmDialog
- [x] 11. StartScreen 구현 (이어서 하기/새로 시작하기 분기, 안내 문구)
- [x] 12. QuestionScreen 구현 (카드 선택, 200ms 전환, reduced-motion 대응, 이전 버튼, 진행률)
- [x] 13. TieBreakScreen 구현 (2개/3개 이상 분기)
- [x] 14. ResultScreen 구현 (처방전 스타일, 점수 막대, 공유/클립보드, 링크 준비중 처리)
- [x] 15. App.tsx에서 화면 상태 머신 연결 및 전체 흐름 통합
- [x] 16. 반응형 점검: 360/390/430px 실제 브라우저에서 가로 스크롤 없음 확인
- [x] 17. 시나리오 수동 점검: 이어서 하기, 새로고침 유지, 재시작 초기화, 답변 변경, 2/3개 동점 결정 화면, 링크 준비중 안내
- [x] 18. `npm run typecheck` / `npm run lint` / `npm run test` / `npm run build` 모두 통과 확인

---

## 9. 구현 완료 현황

Vite + React + TypeScript(strict) 기반으로 구현을 완료했습니다. `npm run typecheck`, `npm run lint`, `npm run test`(14개 테스트 통과), `npm run build`가 모두 오류 없이 통과했으며, 개발 서버를 띄워 360/390/430px 뷰포트에서 시작 → 문항 → 이전 버튼 → 새로고침 유지 → 2개/3개 동점 결정 화면 → 결과 화면(공유/템플릿 링크 준비중/재검사) 흐름을 직접 확인했습니다. GitHub 연동 및 배포는 아직 진행하지 않았습니다.
